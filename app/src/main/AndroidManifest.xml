<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jovanovic.stefan.crudLibrairie">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.jovanovic.stefan.crudLibrairie.UpdateActivity"
            android:parentActivityName="com.jovanovic.stefan.crudLibrairie.MainActivity"/>
        <activity
            android:name="com.jovanovic.stefan.crudLibrairie.AddActivity"
            android:label="Add Book"
            android:parentActivityName="com.jovanovic.stefan.crudLibrairie.MainActivity" />
        <activity
            android:name="com.jovanovic.stefan.crudLibrairie.MainActivity"
            android:label="Book Library">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>